{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json, os, sys; f = json.loads(os.environ.get('CLAUDE_TOOL_INPUT', '{}')); p = str(f.get('file_path', '') if isinstance(f, dict) else ''); b = {'.bin', '.dat', '.wyt', '.wys', '.msa', '.msh', '.trn', '.wav', '.mp3'}; e = os.path.splitext(p)[1].lower(); sys.exit(2 if p and e in b else 0)\""
          }
        ]
      }
    ]
  }
}
